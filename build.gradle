ext {
    env = Dotenv.configure().location("$rootDir").load()
}

wrapper {
    gradleVersion libs.versions.gradle.get()
}

task stage() {
    dependsOn ":app-client:build"
    finalizedBy ":app-server:build"

    doLast {
        print "COMPLETE"
    }
}
